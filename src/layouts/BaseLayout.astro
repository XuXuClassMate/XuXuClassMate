---
import {
  absoluteUrl,
  canonicalPath,
  getCopy,
  hreflangLinks,
  jsonLdGraph,
  languageHref,
  pageHref,
  type Locale,
  type PageId,
} from "../lib/i18n";
import "../styles/global.css";

type Props = {
  locale: Locale;
  page: PageId;
  /** Root `/` English home (legacy URL). */
  rootHome?: boolean;
  noindex?: boolean;
};

const { locale, page, rootHome = false, noindex = false } = Astro.props;
const copy = getCopy(locale);
const meta = copy.meta[page === "home" ? "home" : page];
const canonical = absoluteUrl(canonicalPath(locale, page, rootHome));
const alternates = hreflangLinks(locale, page, rootHome);
const homeHref = locale === "zh" ? pageHref("zh", "home") : "/";
const active = page === "home" ? undefined : page;
const ogTitle = meta.ogTitle ?? meta.title;
const ogImage = absoluteUrl("/images/logo.svg");
const structuredData = noindex ? null : jsonLdGraph(locale, page, rootHome);
---

<!doctype html>
<html lang={locale} data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{meta.title}</title>
    <script is:inline>
      (function () {
        try {
          let theme = localStorage.getItem("xuxu-theme");
          if (theme !== "light" && theme !== "dark") theme = "dark";
          document.documentElement.setAttribute("data-theme", theme);
        } catch {
          document.documentElement.setAttribute("data-theme", "dark");
        }
      })();
    </script>
    {
      noindex ? (
        <meta name="robots" content="noindex, nofollow" />
      ) : (
        <meta
          name="robots"
          content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1"
        />
      )
    }
    {!noindex ? <link rel="canonical" href={canonical} /> : null}
    {
      alternates.map((link) => (
        <link rel="alternate" hreflang={link.hreflang} href={link.href} />
      ))
    }
    <meta name="description" content={meta.description} />
    <meta name="keywords" content={meta.keywords} />
    <meta name="author" content={copy.author} />
    <meta name="theme-color" content="#070b14" />
    <link rel="sitemap" type="application/xml" href="/sitemap.xml" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={copy.brand} />
    <meta property="og:locale" content={locale === "zh" ? "zh_CN" : "en_US"} />
    <meta
      property="og:locale:alternate"
      content={locale === "zh" ? "en_US" : "zh_CN"}
    />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={meta.description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:alt" content={copy.brand} />

    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={ogTitle} />
    <meta name="twitter:description" content={meta.description} />
    <meta name="twitter:image" content={ogImage} />

    <link rel="shortcut icon" href="/images/logo.svg" />
    <link rel="icon" type="image/svg+xml" href="/images/logo.svg" />
    <link rel="apple-touch-icon" href="/images/logo.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600;700&family=Outfit:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    {
      structuredData ? (
        <script
          is:inline
          type="application/ld+json"
          set:html={JSON.stringify(structuredData)}
        />
      ) : null
    }
  </head>
  <body>
    <header class="header" id="siteHeader">
      <div class="container">
        <nav class="nav" aria-label="Primary">
          <a href={homeHref} class="logo">
            <img src="/images/logo.svg" alt={`${copy.brand} logo`} />
            <span class="logo-text">{copy.brand}</span>
          </a>
          <div class="nav-end">
            <button
              type="button"
              class="theme-toggle"
              id="themeToggle"
              aria-label={copy.themeToggle}
              data-label-light={copy.themeToLight}
              data-label-dark={copy.themeToDark}
            >
              <i class="fas fa-moon" aria-hidden="true"></i>
              <span class="theme-toggle-label">{copy.themeToLight}</span>
            </button>
            <button
              id="mobileNavToggle"
              class="mobile-nav-toggle"
              type="button"
              aria-label="Menu"
            >
              <span></span>
              <span></span>
              <span></span>
            </button>
            <div class="nav-links" id="navLinks">
              <a
                href={pageHref(locale, "life")}
                class:list={["nav-item", { active: active === "life" }]}
              >
                {copy.nav.life}
              </a>
              <a
                href={pageHref(locale, "learn")}
                class:list={["nav-item", { active: active === "learn" }]}
              >
                {copy.nav.learn}
              </a>
              <a
                href={pageHref(locale, "work")}
                class:list={["nav-item", { active: active === "work" }]}
              >
                {copy.nav.work}
              </a>
              <a href={languageHref(locale, page)} class="language-switch">
                {copy.languageSwitch}
              </a>
            </div>
          </div>
        </nav>
      </div>
    </header>

    <slot />

    <button
      type="button"
      class="back-to-top"
      id="backToTop"
      aria-label={copy.backToTop}
      title={copy.backToTop}
    >
      ↑
    </button>
    <script src="../scripts/site.ts"></script>
  </body>
</html>
